extends base
include mixins/video


block content
    h2 Welcome here you will see the trending videos
        each info in videos 
            +video(info)
            //video = videos의 객체(array)
        else
            li Sorry nothing found.